# План миграции домена: protrader.systems → protradersystems.com

**Дата:** 19 января 2026  
**Текущий домен:** protrader.systems (Invalid nameservers)  
**Новый домен:** protradersystems.com (Active в Cloudflare)

---

## 📊 Результаты исследования проекта

### Где упоминается старый домен:

| Файл | Строка | Содержимое | Нужно менять? |
|------|--------|------------|---------------|
| `QuizSection.tsx` | 94 | `n8n.protrader.systems` | ✅ Да → `n8n.protradersystems.com` |
| `Run_n8n_with_Cloudflare.bat` | — | Токен привязан к `n8n.protrader.systems` | ✅ Да, создать новый туннель |

### Где домен НЕ упоминается (уже OK):

| Конфигурация | Значение | Статус |
|--------------|----------|--------|
| `.env.local` | Supabase URL, Workers URL | ✅ Не зависит от домена |
| `netlify.toml` | SPA редирект | ✅ Не зависит от домена |
| `index.html` | Мета-теги | ⚠️ Нужно обновить title и OG tags |

---

## 🎯 План миграции

### Этап 1: Настройка нового домена в Cloudflare (15 мин)

- [ ] 1.1 Настроить DNS записи для `protradersystems.com`:
  - `A` или `CNAME` запись для root домена → Netlify
  - `CNAME` для `www` → Netlify
  
- [ ] 1.2 Создать новый Cloudflare Tunnel для `n8n.protradersystems.com`
  - Добавить Public Hostname в существующий туннель `n8n-home-server`
  - Или создать новый туннель

### Этап 2: Обновление кода (10 мин)

- [ ] 2.1 `QuizSection.tsx`: Заменить webhook URL
  ```
  https://n8n.protrader.systems → https://n8n.protradersystems.com
  ```

- [ ] 2.2 `index.html`: Обновить мета-теги
  ```html
  <title>ProTrader Systems — Системный трейдинг</title>
  <meta property="og:title" content="ProTrader Systems" />
  <meta property="og:url" content="https://protradersystems.com" />
  ```

- [ ] 2.3 Обновить `Run_n8n_with_Cloudflare.bat` с новым токеном (после создания туннеля)

### Этап 3: Настройка Netlify (10 мин)

- [ ] 3.1 Добавить `protradersystems.com` как основной домен в Netlify
- [ ] 3.2 Настроить SSL сертификат (автоматически через Netlify или Cloudflare)
- [ ] 3.3 Проверить деплой

### Этап 4: Редирект со старого домена (опционально)

Если `protrader.systems` когда-то заработает:
- [ ] 4.1 Настроить 301 редирект на `protradersystems.com`
- [ ] 4.2 Или оставить как отдельный домен

---

## 🔧 Рекомендованный вариант

**Вариант A: Использовать только `protradersystems.com`**

Преимущества:
- ✅ Уже активен в Cloudflare
- ✅ Проще запомнить (без точки)
- ✅ Меньше конфигурации

Действия:
1. Настроить DNS для Netlify
2. Создать n8n.protradersystems.com туннель
3. Обновить код
4. Забыть про protrader.systems (или позже настроить редирект)

---

## ⚠️ Важные замечания

1. **Brevo (email)**: Если настроен sender domain `protrader.systems`, нужно добавить `protradersystems.com`

2. **n8n Workflows**: В n8n могут быть хардкод URL. Проверить после миграции.

3. **Supabase Auth**: Проверить allowed redirect URLs в настройках Supabase

---

## ✅ Чеклист перед стартом

- [ ] Есть доступ к Netlify аккаунту
- [ ] Есть доступ к Cloudflare для protradersystems.com
- [ ] n8n работает локально

---

## 📝 Следующий шаг

**Начать с Этапа 1.2** — добавить `n8n.protradersystems.com` в существующий Cloudflare Tunnel.

Это можно сделать через Cloudflare Dashboard → Zero Trust → Tunnels → n8n-home-server → Add Public Hostname.
